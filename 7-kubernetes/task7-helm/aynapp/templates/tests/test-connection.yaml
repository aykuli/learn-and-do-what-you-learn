apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aynapp.fullname" . }}-test-connection"
  labels:
    {{- include "aynapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aynapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
